server:
  port: 8080

spring:
  application:
    name: Expense-Tracker

  datasource:
    url: jdbc:h2:mem:jib;DB_CLOSE_DELAY=-1;mode=mysql;NON_KEYWORDS=user
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        connection:
          characterEncoding: utf-8
          CharSet: utf-8
          useUnicode: true
        default_batch_fetch_size: 20
        enable_lazy_load_no_trans: true

  liquibase:
    enabled: false

  sql:
    init:
      platform: h2
      continue-on-error: true

general:
  cors_enabled: false

jwt:
  secret: dvd25vQuuiPZvi46advjewu
  token_validity_in_minutes: 480  # 8 * 60
  refresh_token_validity_in_minutes: 1440 # 24 * 60
